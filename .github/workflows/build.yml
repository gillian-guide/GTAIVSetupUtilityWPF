name: Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '6.0.x'
        
    - name: Restore dependencies
      run: dotnet restore GTAIVSetupUtilityWPF.csproj -r win-x64
      
    - name: Build
      run: dotnet build GTAIVSetupUtilityWPF.csproj --configuration Release --no-restore -r win-x64
      
    - name: Publish
      run: dotnet publish GTAIVSetupUtilityWPF.csproj --configuration Release --output ./publish --no-build --self-contained true -p:PublishSingleFile=true -r win-x64
      
    - name: Create artifact directory
      run: |
        New-Item -ItemType Directory -Force -Path ./artifact
        Copy-Item -Path ./publish/* -Destination ./artifact/ -Recurse
      shell: pwsh
      
    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: GTAIVSetupUtilityWPF-${{ github.sha }}
        path: ./artifact/
        retention-days: 90
